import React from 'react';
import './Left.css';

interface LeftProps {
    className: string;
}

const Left: React.FC<LeftProps> = ({className}) => {
    return (
        <div className={className}>
            //todo
        </div>
    );
}

export default Left;
